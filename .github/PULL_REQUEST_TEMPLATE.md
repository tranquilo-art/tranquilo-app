## What and why

<!-- What does this change, and what problem does it solve? Link the issue
     it was discussed in first (see CONTRIBUTING.md's "How to Contribute"). -->

## Testing

<!-- Both suites must pass locally before this merges -- CI here is
     unreliable by design (see CONTRIBUTING.md), so a green CI check is not
     the gate. -->

- [ ] `bun run test:all` passes (vitest + Playwright)
- [ ] `bun run typecheck` passes, if TypeScript changed
- [ ] Screenshots or a screen recording attached, if this changes UI/UX

## Anything a reviewer should know

<!-- A live-database dependency reviewers can't verify, a deliberate
     deviation from an existing convention, a decision you want a second
     opinion on before it lands -- optional, delete if none. -->
