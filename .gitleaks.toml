# CI secret scan config (.github/workflows/security.yml). Extends
# gitleaks' own default ruleset rather than replacing it -- see
# https://github.com/gitleaks/gitleaks/blob/master/config/gitleaks.toml
# for what "default" actually checks.
[extend]
useDefault = true

[allowlist]
description = "Known non-secret, intentionally-public identifiers"
regexes = [
  # Cloudflare Web Analytics beacon token (src/analytics/cloudflareBeacon.ts)
  '''7e81d611e67440d3ab668bdfb15ba2b9''',
  # PostHog project token placeholder (src/analytics/posthogDraft.ts) --
  '''phc_REPLACE_ME''',
]
