// "hero" is a fixed, hand-curated itemIds list; "rule" is a generic filter
// matched against item fields at render time, gated by minItems.
export enum ShelfType {
  Hero = "hero",
  Rule = "rule",
}
