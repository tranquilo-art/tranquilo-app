// pages/get-involved.html's entry point. The page is static content, so
// there's nothing to wire up yet -- this file exists so vite.config.mts's
// build entry has a script block to point at, same as every other
// pages/*.html.
export {};
