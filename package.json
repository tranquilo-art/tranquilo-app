{
  "name": "tranquilo-poc-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "description": "Dependency manifest for the /api serverless functions (share-link meta tags, OG image generation, collection submission email, analytics ingestion) plus dev-only test tooling (Vitest) and the Vite/TypeScript toolchain for the frontend (see CONVENTIONS.md). The live deploy serves Vite's multi-page `dist/` build (vercel.json's outputDirectory/buildCommand) -- `bun run build` bundles index.html and every pages/*.html entry, each referencing its .ts sources directly. Vercel installs `dependencies` automatically at deploy time for the /api functions; `devDependencies` never ships. `prisma` is migrations-only -- schema.prisma/prisma.config.ts drive `bun run db:migrate:*`, but there is no @prisma/client and no query builder; every actual query stays raw SQL via @neondatabase/serverless.",
  "packageManager": "bun@1.3.11",
  "scripts": {
    "dev": "bun x vite",
    "vercel": "bun x vercel dev",
    "build": "bun run generate-config && bun run generate-env && bun x vite build",
    "preview": "bun run build && bun x vite preview",
    "generate-config": "bun scripts/generate-config.mts",
    "generate-env": "bun scripts/generate-env.mts",
    "humans": "bun scripts/generate-humans-txt.mts",
    "version:bump": "bun scripts/bump-version.mts",
    "changelog": "bun scripts/generate-changelog.mts",
    "db:migrate:dev": "prisma migrate dev --skip-generate",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:migrate:status": "prisma migrate status",
    "typecheck": "bun run generate-config && tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun x vitest run",
    "test:watch": "bun x vitest",
    "test:e2e": "bun run build && bun x playwright test",
    "test:e2e:changed": "bun scripts/select_e2e_specs.mts",
    "test:e2e:topbar": "bun scripts/select_e2e_specs.mts --group topbar",
    "test:e2e:search": "bun scripts/select_e2e_specs.mts --group search",
    "test:e2e:collection": "bun scripts/select_e2e_specs.mts --group collection",
    "test:e2e:storyline": "bun scripts/select_e2e_specs.mts --group storyline",
    "test:e2e:feed": "bun scripts/select_e2e_specs.mts --group feed",
    "test:e2e:lightbox": "bun scripts/select_e2e_specs.mts --group lightbox",
    "test:e2e:detail": "bun scripts/select_e2e_specs.mts --group detail",
    "test:e2e:pages": "bun scripts/select_e2e_specs.mts --group pages",
    "test:e2e:misc": "bun scripts/select_e2e_specs.mts --group misc",
    "test:all": "bun run build && bun x vitest run && bun x playwright test"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@sentry/node": "^10.69.0",
    "@vercel/blob": "^2.7.0",
    "@vercel/og": "^1.0.2",
    "posthog-js": "^1.430.2",
    "sharp": "^0.35.4",
    "svix": "^2.3.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "@electric-sql/pglite": "^0.5.8",
    "@playwright/test": "^1.62.1",
    "@types/bun": "^1.4.1",
    "@types/node": "^26.2.0",
    "@types/ws": "^8.18.1",
    "@vercel/node": "^12.0.0",
    "posthtml": "^0.16.7",
    "posthtml-extend": "^0.6.5",
    "prisma": "7.10.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^3.0.0"
  }
}
